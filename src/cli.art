import ./{internal/runner}!
import ./{internal/finder}!

print args

tryOr: $[action :block alt :any][
    (throws? [val:] ++ action)? -> alt -> val
]

files: args\values ?? append 
    findTests "test" 
    findTests "tests"

runTests
    files