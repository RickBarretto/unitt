import {unitt}!
code:  import.lean {code}


test "I must never run" [
    assert -> true
]

test "I must never run" [
    assert -> true
    assert -> true
]

test "I must never run" [
    assert -> false
]

test "I must never run" [
    assert -> false
]